/** 9:16 auto-reframe with face tracking — see commands.md Phase 5.1. */
export async function reframe9x16(
  _inputPath: string,
  _outputPath: string,
): Promise<void> {
  throw new Error('reframe9x16 not implemented — see Phase 5.1')
}
