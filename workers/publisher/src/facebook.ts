/** Facebook + Instagram Reels publishing — see commands.md Phase 7.2. */
export async function publishToFacebook(_clipId: string): Promise<void> {
  throw new Error('publishToFacebook not implemented — see Phase 7.2')
}
