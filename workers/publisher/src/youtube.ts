/** YouTube Shorts publishing — see commands.md Phase 7. */
export async function publishToYouTube(_clipId: string): Promise<void> {
  throw new Error('publishToYouTube not implemented — see Phase 7')
}
