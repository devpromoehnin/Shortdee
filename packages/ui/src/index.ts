/**
 * @clipdee/ui — shared UI utilities and (later) components.
 *
 * For now this exports the `cn` class-merge helper. shadcn/ui components
 * live in `apps/web/src/components/ui` until they need to be shared.
 */
export { cn } from './cn'
